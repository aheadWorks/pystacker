from aiohttp import web
import re
from ..utils.common import deep_update

#from .plugins import LoggerPlugin, LogAdvanced, PluginsRoot




def init_plugins(app: web.Application):
    pass
    #map(lambda x: x.mount(), [LogAdvanced(), LoggerPlugin()])

